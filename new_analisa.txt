====================================================================
AETHER — Agent Report UI Fix: tampilkan isi report UTUH (tanpa truncate)
====================================================================
Scope: FRONTEND ONLY (Agent Activity / Agent Report). Tidak ada perubahan
logic Python/backend, Agent execution, TaskExecutor, logging .aether/log,
Report API, report generation, maupun Task lifecycle.

--------------------------------------------------------------------
1. FILE YANG DIUBAH
--------------------------------------------------------------------
1) web/frontend/src/components/AgentActivity.vue
   - import renderMarkdown dari shared renderer "../markdown.js"
     (renderer yang SAMA dengan ReportViewer).
   - case "task_completed": sekarang mengambil data.result (Agent Report
     final) dan merender penuh sebagai Markdown (reportHtml). Tidak ada
     truncate/ellipsis pada isi report.
   - Template: menambahkan blok <div class="md act-report" v-html=...>
     di dalam baris timeline (di dalam scroll container .act-body).

2) web/frontend/src/styles.css
   - Menambahkan rule .act-report (container report di timeline) + reset
     margin elemen markdown terakhir. Tidak ada clipper/line-clamp/max-height
     pada report; overflow ditangani container .act-body yang memang scrollable.

Tidak ada file lain yang disentuh. Tidak ada file Python/backend yang diubah.

--------------------------------------------------------------------
2. PENYEBAB TRUNCATION
--------------------------------------------------------------------
Temuan inspeksi (bukan asumsi):

a) String literal "[readmore]" TIDAK ADA di seluruh codebase — baik frontend
   (web/frontend/src) maupun backend (src, web/django_app). Diverifikasi dengan
   search_code ("readmore", "read more", "[readmore]") dan git history
   (`git log -S readmore` -> kosong). Jadi tidak ada kode UI yang memotong
   report dengan penanda tersebut.

b) Isi report final tersimpan UTUH di log: event task_completed memiliki
   data.result lengkap (contoh task 94442487... = 2014 char; 0fc08d95... =
   1376 char). Report API membaca task_completed.data.result apa adanya.

c) Teks yang tampak terpotong dengan "…" pada titik penyelesaian ("AGENT —
   Task completed") adalah pesan/commentary terakhir Agent, yang dipotong oleh
   BACKEND menjadi 600 char + "…" di:
       src/agent_ai/core/orchestrator.py :: _extract_commentary()
       -> `if len(text) > 600: text = text[:600].rstrip() + "…"`
   Sumber truncation ini ada di layer Agent execution (backend) sehingga TIDAK
   boleh/tidak diubah sesuai batasan tugas.

d) Di frontend, sebelum perbaikan, event task_completed pada Agent Activity
   hanya merender teks label "Task completed" dan TIDAK pernah merender isi
   report. Tidak ditemukan truncation/ellipsis/clamp pada jalur display report
   (ReportViewer memang sudah menampilkan full + scrollable).

--------------------------------------------------------------------
3. PERBAIKAN YANG DILAKUKAN
--------------------------------------------------------------------
- Agent Report final (task_completed.data.result) kini ditampilkan UTUH di alur
  Agent Activity pada baris "AGENT — Task completed", memakai Markdown renderer
  bersama (web/frontend/src/markdown.js) — sama seperti ReportViewer.
- Tidak ada truncate/ellipsis dan tidak ada "[readmore]".
- Report panjang mengikuti tinggi container dan dapat di-scroll melalui
  .act-body (overflow-y: auto) yang sudah ada — mekanisme scroll tidak dihapus.
- Markdown yang sudah didukung (heading, list, code block, blockquote, hr,
  inline) tetap dirender dengan behavior yang sama (tidak diubah).
- Report tetap berada dalam alur Agent Activity / Task History seperti sekarang
  (ReportViewer modal + tombol Report tidak diubah).

--------------------------------------------------------------------
4. KONFIRMASI: TIDAK ADA PERUBAHAN PYTHON/BACKEND
--------------------------------------------------------------------
- Tidak ada file .py yang dibuat/diubah/dihapus.
- Tidak menyentuh: AgentRuntime, TaskExecutor, TaskState/state machine,
  SessionStore, SSE contract, Activity/Report/Task History API, .aether/log,
  report generation, maupun Task lifecycle.
- Perubahan hanya pada 2 file frontend (lihat bagian 1).

--------------------------------------------------------------------
VALIDATION
--------------------------------------------------------------------
- Production build OK: `node node_modules/vite/bin/vite.js build`
  -> 30 modules transformed, 0 errors.
- Bundle output memuat reportHtml + class "md act-report" dan TIDAK memuat
  "readmore".
- scripts/check_workbench.py step [2] gagal karena assertion lama
  ("Agent Workspace"/"Recent Projects") — PRE-EXISTING, tidak terkait perubahan
  ini. Assertion step [8] (event agent_commentary/tool_called/tool_completed/
  observation_received + label AGENT/TOOL/RESULT/OBSERVATION) tetap terpenuhi.
- Tidak ada perubahan pada penanganan event lain (tool_called, tool_completed,
  observation_received, task_failed, task_cancelled).

--------------------------------------------------------------------
CATATAN
--------------------------------------------------------------------
- Truncation 140 char pada summarizeObservation() (baris OBSERVATION tool)
  sengaja TIDAK diubah: itu untuk ringkasan observasi tool, bukan isi Agent
  Report, dan tugas meminta penyesuaian truncation "hanya untuk Agent Report".
- Karena sumber "…" (600 char) berada di backend dan di luar batas tugas,
  perbaikan frontend difokuskan menampilkan report final yang UTUH dari
  task_completed.data.result (sumber isi yang lengkap).
