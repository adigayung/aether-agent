AETHER Workbench — Benahi & Redesain Panel CHANGES (kartu satu baris + accordion diff)
========================================================================================

TASK
(1) Perbaiki rendering panel CHANGES (Workbench right column) yang tampak kacau
    (kartu oversize, teks terpotong, scrollbar bertumpuk/nested).
(2) Redesain kartu perubahan menjadi SATU BARIS per file yang ringkas + interaktif
    (diff muncul saat diklik / accordion).
(3) Beri CHANGES ruang lebih (~40%) dan pendekkan File Explorer sedikit.
Frontend-only. Tidak menyentuh backend. Tidak merusak kontrak scroll Explorer.

--------------------------------------------------------------------------------
AKAR MASALAH (ditemukan dari source, bukan asumsi)
--------------------------------------------------------------------------------
1. Kartu oversize:
   `.file-ico` berukuran 46x46px + `.file-body` dua baris (nama + meta/tag)
   + padding 11px 13px  => tiap kartu tinggi ~68-72px. Dengan 3 file, daftar
   melampaui tinggi blok (max-height:30%) sehingga kartu terakhir terpotong.
2. Scroll ganda (nested):
   `.cp-diff-body pre { max-height:220px; overflow-y:auto }` membuat scrollbox
   KEDUA di dalam scroll owner `.changes-body` => muncul scrollbar bertumpuk.
3. Gejala "kacau" pada diff:
   diff path dirender ulang padahal nama file sudah tampil di baris, dan
   container diff muncul tanpa accordion (selalu tampil begitu file diklik),
   memperbesar kemungkinan overflow horizontal pada diff panjang/sempit.
4. Alokasi tinggi: CHANGES dibatasi `max-height:30%` sehingga ruang daftar sempit.

--------------------------------------------------------------------------------
PERUBAHAN YANG DITERAPKAN (frontend-only)
--------------------------------------------------------------------------------
File: web/frontend/src/components/ChangesPanel.vue
- Kartu diubah menjadi SATU BARIS per file:
    [caret ▸/▾] [status A/M/D berwarna] [nama file ellipsis] [+N / -N] [tombol Edit]
- Accordion: `expandedIndex` (ref). Klik baris => buka/tutup diff-nya. Hanya SATU
  baris terbuka pada satu waktu. Diff TIDAK dirender saat default (tertutup).
- Diff dirender di dalam aliran `.changes-body` (bukan scrollbox terpisah) dan
  memakai wrap (`pre-wrap` + `overflow-wrap:anywhere`) sehingga tidak ada
  horizontal overflow.
- Status indikator: A=added (hijau), M=modified (kuning), D=removed (merah);
  fallback "•" untuk kind tak dikenal. `tagLabel` tetap dipakai sebagai tooltip.
- Menghapus state `selected`/`selectedDiff` + blok `cp-diff` lama (diganti
  struktur accordion). Tidak ada perubahan pada data/props/emit `open-file`.

File: web/frontend/src/styles.css
- `.file-row`: dipadatkan (gap 9px, padding 6px 9px, radius 9px) + state
  hover/open lembut (border + background accent transparan, non-neon).
- Elemen baru: `.file-caret`, `.file-status` (+ .created/.modified/.failed/.idle),
  `.file-name` (min-width:0 => ellipsis satu baris), `.file-stat` (.st-add/.st-del).
- `.file-edit` diperkecil 28px -> 24px.
- Menghapus aturan mati: `.file-body`, `.file-ico`, `.file-meta`, `.file-tags`,
  `.file-path`, `.file-row.selected`, `.cp-diff*`.
- Diff baru: `.file-diff` + `.file-diff-body pre` (TANPA max-height/overflow =>
  satu scroll owner) + `.file-diff-note`.
- Alokasi tinggi: `.ws-col.right > .block:not(.explorer-block):not(.wb-queue)`
  `max-height: 30% -> 40%` (CHANGES lebih tinggi; Explorer otomatis menyusut
  karena flex:1 1 auto sisa).
- Fallback media query <=1080px: `.explorer-block` max-height `60vh -> 52vh`
  (Explorer sedikit lebih pendek), CHANGES `240px -> 260px`.
- `.tag` (dipakai App.vue) TIDAK diubah.

KONTRAK YANG DIPERTAHANKAN
- `.explorer` tetap SATU-SATU scroll owner: flex:1 1 auto; min-height:0;
  overflow-y:auto; overflow-x:hidden (tidak diubah).
- `.ex-head { flex:0 0 auto }`, `.explorer-block` (flex:1 1 auto; min-height:0;
  overflow:hidden), `.ws-body { grid-template-rows: minmax(0,1fr) }` tidak diubah.
- `.wb-queue` (TASKS) tetap dikecualikan dari selektor aturan CHANGES.
- `.changes-body` tetap SATU scroll owner untuk CHANGES (tidak ada scroll ganda).
- Struktur/markup `.ex-head` (ex-head/ex-head-title/ex-head-ws/ex-icon-btn) dan
  perilaku context menu/open/rename/delete TIDAK disentuh (FileExplorer.vue
  tidak dimodifikasi).
- Tidak ada komponen/subsistem/dependency baru; tidak ada file backend/Python.

--------------------------------------------------------------------------------
VALIDASI
--------------------------------------------------------------------------------
- Build frontend (dari web/frontend):
    `node node_modules/vite/bin/vite.js build`
  => 32 modules, 0 error. Bundle baru: dist/assets/index-CzZbGHeD.css.
- Bundle produksi memuat aturan baru: `.changes-list`, `.file-status`,
  `.file-diff-body pre`, `max-height:40%`, dan kontrak `.explorer` serta
  `.ws-body{grid-template-rows:minmax(0,1fr)}` tetap utuh (dicek via grep bundle).
- `python scripts/check_workbench.py` => 12/12 PASS, termasuk
  "[12] Explorer daftar file scroll owner OK -> header tetap, hanya daftar scroll".
- `git status --porcelain` => hanya file frontend + new_analisa.txt berubah.
- `git diff --stat -- src web/django_app` => KOSONG (tidak ada perubahan backend).

--------------------------------------------------------------------------------
BUKTI BACKEND TIDAK TERSENTUH
--------------------------------------------------------------------------------
git diff --stat -- src web/django_app  -> (kosong)
Tidak ada perubahan pada: API, task execution, logging, SessionStore/SSE,
AgentRuntime, TaskExecutor, TaskState, atau file Python apa pun.

--------------------------------------------------------------------------------
CATATAN
--------------------------------------------------------------------------------
- `web/frontend/src/components/ConsultantChat.vue` tampil sebagai "modified" di
  git status, namun itu berasal dari perubahan sesi sebelumnya (di luar scope
  task ini) dan tidak disentuh pada task ini.
- Verifikasi visual interaktif (klik buka/tutup diff, scroll daftar) tetap
  disarankan di browser; validasi otomatis di sini mencakup build + verifier
  kontrak scroll + inspeksi bundle.
