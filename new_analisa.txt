=====================================================================
SUMMARY — Provider/Model per-Task di Card Task Proposal + Tombol Copy
Frontend-only. Backend TIDAK diubah (perubahan tidak diperlukan).
=====================================================================

TASK
----
1) Kembalikan kontrol Provider + Model DI DALAM card Task Proposal Consultant
   Chat, yang dipakai untuk MENJALANKAN task (Run Task) — TERPISAH dari
   dropdown di header modal yang tetap untuk percakapan (chat).
2) Tambahkan tombol Copy pada card Task Proposal agar konsisten dengan card
   chat lain.

DEFAULT: provider/model card = pilihan header saat pertama; setelah diubah
manual di card, pilihan card INDEPENDEN dari header (dan sebaliknya).

FILES CHANGED
-------------
- web/frontend/src/components/ConsultantChat.vue
- web/frontend/src/App.vue
- web/frontend/src/styles.css
- new_analisa.txt (ringkasan ini)
- web/frontend/dist/** (hasil rebuild produksi; artefak, bukan source)

Tidak ada perubahan pada:
- src/agent_ai/** (Python core, runtime, orchestrator, tools, consultant)
- web/django_app/** (Django API/backend: services.py, views.py, urls.py)
- .aether/log, SessionStore/SSE, Activity/Report API, ReportViewer
Terverifikasi: `git status --porcelain` hanya menampilkan
  M new_analisa.txt
  M web/frontend/src/App.vue
  M web/frontend/src/components/ConsultantChat.vue
  M web/frontend/src/styles.css
(tidak ada file .py).

TEMUAN VERIFIKASI (sebelum coding)
----------------------------------
1) Backend SUDAH mendukung override provider/model per-task — TIDAK perlu
   perubahan backend:
   - `GatewayService.create_task(task, project_id, metadata)` (services.py ~L770)
     memvalidasi `metadata.provider_instance_id`/`model_id` via
     `_validate_provider_selection()` (services.py ~L1054) dan meneruskannya ke
     `task_executor.run(..., provider_instance_id=..., model_id=...)`
     (services.py ~L1015/L1027).
   - `views.py::tasks()` (POST /api/tasks) sudah meneruskan `metadata` apa adanya.
   - `api.js::createTask(task, projectId, metadata)` sudah mengirim `metadata`.
   - `App.vue::submitTask()` sudah merakit metadata `provider_instance_id` /
     `model_id` dari state global.
   => Cukup menambahkan kemampuan override (opsional) pada jalur frontend.
   TIDAK ada blocker backend.

2) Jalur lama Run Task hanya mengirim teks:
   `ConsultantChat.runTask()` -> `emit('run-task', proposal)`
   -> `App.vue::runConsultantTask(text)` -> `submitTask(text)`.
   Card `.consultant-proposal` hanya punya tombol Run Task; dropdown
   `.consultant-selects.inline` pernah ada lalu dihapus (riwayat user).

3) Pola copy yang SUDAH ADA (dipakai ulang, tidak membuat baru):
   `.cmsg-actions` + `.copy-btn`, ikon SVG copy -> centang, label
   Copy -> Copied, `navigator.clipboard.writeText(...).catch(()=>{})`,
   reset ~1.4s.

DESAIN / PERUBAHAN
------------------
ConsultantChat.vue:
- State runner card (independen dari header):
  `proposalProviderInstanceId`, `proposalModelId`, `proposalTouched`.
  Watcher `immediate` menyinkronkan nilai dari prop header SELAMA belum
  disentuh user; setelah disentuh (`proposalTouched=true`) card independen.
- `proposalModelOptions` (filter model mengikuti provider card) via `modelsFor`.
- Handler `onProposalProviderChange` (reset model ke model valid pertama) dan
  `onProposalModelChange`.
- `runTask()` kini emit payload { text, providerInstanceId, modelId }.
- Copy card: `proposalCopied` + `copyProposal()` (pola .copy-btn, reset ~1.4s).
- Template card: `.cp-foot` = `.cp-runner` (2 <select class="input-a">:
  Provider + Model, di-DISABLE saat runDisabled) + tombol Run Task di kanan;
  lalu baris `.cmsg-actions.cp-actions` berisi tombol Copy di kiri-bawah.
  Card tetap DI DALAM `.consultant-messages` (kontrak layout tidak berubah).

App.vue:
- `submitTask(text, overrideProviderInstanceId=null, overrideModelId=null)`:
  override MENANG atas pilihan global; bila kosong -> perilaku default.
- `runConsultantTask(payload)`: menerima payload object {text, providerInstanceId,
  modelId} ATAU string lama (backward compatible), lalu meneruskannya ke
  `submitTask(text, providerId, modelId)`. Pilihan header (chat) TIDAK diubah.

styles.css:
- `.cp-foot` -> `justify-content: space-between` (runner kiri, Run Task kanan).
- Baru: `.cp-runner`, `.cp-select`, `.cp-select .cs-label`, `.cp-select .input-a`,
  `.cp-actions`.
- Tidak menyentuh `.consultant-m`, `.consultant-messages` (scroll owner),
  `.consultant-foot` (sibling flex-shrink:0), maupun kontrak `.copy-btn` chat.

KONTRAK YANG DIPERTAHANKAN
--------------------------
- `.consultant-messages` tetap satu-satunya scroll owner
  (flex:1 1 auto; min-height:0; overflow-y:auto).
- `.consultant-foot` tetap sibling dengan flex-shrink:0.
- `.consultant-proposal` tetap DI DALAM `.consultant-messages`.
- Panel LLM MODEL (aside.consultant-models / cm-side-*) TIDAK dihidupkan kembali.
- Fitur lain tidak berubah: mode Quick/Investigate, attach image,
  Enter=newline / Shift+Enter=send, auto-scroll, tool activity, Run Task.
- Tidak ada dropdown dobel di header.

VALIDASI
--------
- Build frontend: dari web/frontend
  `node node_modules/vite/bin/vite.js build` -> 32 modules, 0 error.
  Bundel membuktikan:
  - emit run-task = {text, providerInstanceId, modelId}.
  - submitTask memakai override: `provider = override || global`,
    `model = override || global`.
  - CSS `.cp-runner`/`.cp-select`/`.cp-actions` dan tombol
    "Copy task proposal" ada di bundel production.
- Verifikasi Consultant: `python scripts/check_consultant.py` -> SEMUA PASS
  (core, Bible, registry per-mode, boundary, session, gateway HTTP,
  Task Proposal -> create_task, frontend mode).
- `git status --porcelain` -> tidak ada file backend/Python berubah.

KONFIRMASI BACKEND
------------------
TIDAK ADA perubahan backend. Override provider/model per-task sudah didukung
backend sehingga hanya jalur frontend (consultant chat -> submitTask ->
createTask metadata) yang disesuaikan. Default behavior task tanpa override
tidak berubah.
