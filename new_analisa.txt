================================================================================
TASK: Panel "TASK LIST / TASK QUEUE" di area Consultant AETHER
      (tahap UI Task List + integrasi Run Task -> createTask -> tampil di TASKS)
================================================================================
Mode   : Investigate (verifikasi source sebelum perubahan) + additive, minimal diff
Scope  : Frontend (Vue) + backend minimal (field & endpoint antrian, additive)
Status : SELESAI — build 0 error, 8 verifier lulus, Agent reasoning TIDAK berubah


--------------------------------------------------------------------------------
1. LANGKAH 0 — INSPEKSI TERBATAS (HASIL, TIDAK ADA PENYIMPANGAN)
--------------------------------------------------------------------------------
Dikonfirmasi ulang di source (bukan asumsi):
- web/frontend/src/App.vue :: runConsultantTask hanya memanggil submitTask(text);
  modal Consultant TIDAK ditutup (perbaikan lama tetap berlaku). Handler
  @run-task terpasang di <ConsultantChat> (baris ~1052).
- web/frontend/src/api.js :: createTask -> POST /api/tasks; listTaskHistory ->
  GET /api/tasks/history. BELUM ada endpoint queue.
- web/django_app/api/services.py :: GatewayService.create_task ->
  _start_execution (satu-satunya pemanggil) -> execution.run_in_background
  (satu-satunya Thread di gateway). list_tasks() = urutan insert dict (FIFO).
- TaskRecord.status = string bebas (prepared/running/completed/failed/cancelled);
  BUKAN enum TaskStatus core (CREATED/PREPARING/PLANNING/RUNNING/VALIDATING/
  COMPLETED/FAILED/CANCELLED, TERMINAL_STATUSES={COMPLETED,FAILED,CANCELLED}).
- Pola context menu (.ctx-menu position:fixed; z-index:1000; tutup saat klik luar
  / Escape) ada di FileExplorer.vue -> dipakai ulang.
- Kontrak modal Consultant: .consultant-messages = scroll owner, .consultant-foot
  = sibling flex-shrink:0, .consultant-proposal di dalam .consultant-messages.
  Ini TIDAK diubah (lihat §4).

Tidak ditemukan komponen queue existing, endpoint queue existing, maupun cara
state dikirim yang berbeda dari asumsi -> desain dapat dijalankan tanpa koreksi.


--------------------------------------------------------------------------------
2. DESAIN & KEPUTUSAN
--------------------------------------------------------------------------------
- SATU queue GLOBAL AETHER. Tidak ada TaskManager/queue subsystem kedua.
  Sumber data tetap TaskRecord in-memory yang sama dengan GET /api/tasks.
- Pada tahap ini BELUM ada scheduler serial. queue_state (pending/running/
  disabled/done) adalah PROYEKSI UI + niat user (disable = jangan dieksekusi).
- `queue_state` disimpan sebagai atribut BARU pada TaskRecord (dan `queue_order`
  untuk FIFO + Move Up/Down). TIDAK ditambahkan ke enum TaskStatus core maupun
  TERMINAL_STATUSES -> TaskState/TaskLifecycle/.aether/log/SSE/history aman.
- Disable != Cancel. Stop task RUNNING tetap memakai cancel_task +
  CancellationToken existing (tidak ada mekanisme cancel kedua). RUNNING tidak
  dapat Disable/Move/Remove -> ditolak HTTP 400.
- queue_state disinkronkan dari lifecycle: status "running" -> queue_state
  "running"; terminal (completed/failed/cancelled) -> queue_state "done"
  (dilakukan di _update_task_status di bawah lock).
- Verifier baru: scripts/check_task_queue.py (9 pemeriksaan, semua lulus).


--------------------------------------------------------------------------------
3. PERUBAHAN FILE
--------------------------------------------------------------------------------
Backend (ADDITIVE — 0 baris dihapus, lihat git diff --numstat):
- web/django_app/api/services.py:
    * TaskRecord: field baru queue_state ("pending" default) + queue_order.
      to_dict() menyertakan queue_state & queue_order.
    * GatewayService.__init__: counter _queue_seq.
    * create_task(): set record.queue_order di dalam lock (FIFO monotonic).
    * _update_task_status(): sinkronisasi queue_state (running/done).
    * list_queue(), set_queue_state(task_id, state), move_task(task_id, dir),
      remove_task(task_id). Semua lock-protected; move/disable/remove hanya
      untuk non-running; remove bukan cancel.
- web/django_app/api/views.py:
    * task_queue (GET), task_queue_disable, task_queue_enable, task_queue_move,
      task_queue_remove (POST).
- web/django_app/api/urls.py:
    * tasks/queue + sub-route disable|enable|move|remove. SEMUA literal route
      "tasks/queue" diletakkan SEBELUM "tasks/<str:task_id>" (anti-shadow).
      Kebab-case sesuai konvensi repo. Kontrak endpoint existing TIDAK diubah.

Frontend:
- web/frontend/src/api.js:
    * listTaskQueue, disableQueueTask, enableQueueTask, moveQueueTask,
      removeQueueTask (client tipis ke endpoint baru).
- web/frontend/src/components/QueuePanel.vue (BARU):
    * Panel TASKS: header collapsible (caret + "TASKS [n]" + task count +
      indikator dot running berdenyut). List item: ikon status (● RUNNING,
      ① PENDING, ◌ DISABLED), label status, posisi queue untuk pending, teks
      task (ellipsis), short id.
    * Right-click context menu (reuse .ctx-menu): RUNNING -> Stop, View Task;
      PENDING -> Move Up, Move Down, Disable, View Task, Remove;
      DISABLED -> Move Up, Move Down, Enable, View Task, Remove.
    * Remove pakai confirmation (teks "This task has not started yet.").
    * Refresh: polling ringan (5s) + prop refreshKey dari parent (SSE terminal).
- web/frontend/src/components/ConsultantChat.vue:
    * Import QueuePanel; prop baru queueRefreshKey; emit baru stop-task,
      view-task.
    * Struktur modal dipertahankan: header + .consultant-modes lalu
      .consultant-body (flex row) berisi .consultant-chat (kolom: messages +
      error + foot) dan <QueuePanel class="consultant-queue">.
    * .consultant-messages tetap SATU-SATUNYA scroll owner di kolom chat;
      .consultant-proposal tetap DI DALAM .consultant-messages.
- web/frontend/src/App.vue:
    * State queueRefresh; increment setelah Run Task (runConsultantTask) dan
      setelah submitTask (Agent Input), serta pada event terminal
      task_completed/task_failed/task_cancelled.
    * stopQueueTask(taskId) -> cancelTask existing; viewQueueTask(t) ->
      openHistoryTask(task.id) (Activity/Report API existing).
    * Prop :queue-refresh-key + handler @stop-task/@view-task ke ConsultantChat.
- web/frontend/src/styles.css:
    * .consultant-body (flex row, min-height:0) + .consultant-chat (flex column).
      Kontrak scroll modal TIDAK berubah (messages = scroll owner, foot =
      flex-shrink:0, proposal di dalam messages).
    * .consultant-m diperlebar 860px -> 1120px (94vw) agar panel bisa di samping.
    * Blok CSS .consultant-queue + seluruh styling panel (q-head, q-item,
      q-state, scrollbar tipis tema AETHER) + media query <=900px (panel turun
      ke bawah chat).


--------------------------------------------------------------------------------
4. BATAS ARSITEKTUR (DIPATUHI)
--------------------------------------------------------------------------------
- Agent reasoning / execution loop / tool policy / provider / Consultant
  reasoning / Project Bible / Project Intelligence: TIDAK diubah.
- .aether/log writer, SessionStore/SSE, runtime _lifecycle_finalize: TIDAK
  disentuh.
- Enum TaskStatus core / TERMINAL_STATUSES: TIDAK diubah (queue_state terpisah).
- Kontrak layout modal Consultant: dipatuhi.
- Kontrak endpoint existing (tasks, tasks/history, activity, report, events,
  consultant/*): tidak diubah (hanya penambahan endpoint baru).


--------------------------------------------------------------------------------
5. VERIFIKASI
--------------------------------------------------------------------------------
- Frontend build: `node node_modules/vite/bin/vite.js build` (dari web/frontend)
  -> 32 modules transformed, 0 error. Bundle memuat QueuePanel, "TASKS",
  .consultant-queue/.consultant-body/.consultant-chat, dan fungsi queue API.
- py_compile services.py/views.py/urls.py: OK.
- Verifier (semua LULUS):
    * scripts/check_task_queue.py (baru) — 9/9
    * scripts/check_django_gateway.py
    * scripts/check_cancellation.py
    * scripts/check_task_lifecycle.py
    * scripts/check_consultant.py
    * scripts/check_web_runtime.py
    * scripts/check_consultant_vision.py
    * scripts/check_aether_store.py
- git status: hanya file terkait (services.py, views.py, urls.py, App.vue,
  api.js, ConsultantChat.vue, styles.css) + 2 file baru (QueuePanel.vue,
  check_task_queue.py). git diff --numstat -- "*.py" = 136/24/58 insertions,
  0 deletions (additive).


--------------------------------------------------------------------------------
6. KETERBATASAN / SISA PEKERJAAN (TAHAP BERIKUTNYA)
--------------------------------------------------------------------------------
- Belum ada SCHEDULER SERIAL (concurrency=1). Saat ini task dari Run Task
  tetap dieksekusi via daemon thread (perilaku lama tidak diubah pada tahap UI
  ini). queue_state "disabled" belum benar-benar mencegah eksekusi karena belum
  ada scheduler yang memilih task; ini akan ditegakkan ketika scheduler dibuat
  (titik pemasangan sudah siap: create_task -> _start_execution).
- State antrian in-memory (hilang saat restart) — sama seperti TaskRecord.
- Task Queue di Agent Workbench BELUM dikerjakan (di luar scope tahap ini);
  panel Consultant membaca queue global yang sama sehingga nanti bisa dipakai
  ulang.


================================================================================
TASK (sebelumnya): Perbaiki posisi Task Proposal card di Consultant Chat agar
      mengikuti message flow (card naik ke atas saat ada balasan baru)
================================================================================
Mode  : Investigate (verifikasi DOM/CSS sebelum perubahan) + perbaikan minimal
Scope : Frontend-only
Status: SELESAI — build 0 error, backend tidak tersentuh


--------------------------------------------------------------------------------
1. AKAR MASALAH (ROOT CAUSE) — TERVERIFIKASI DARI SOURCE
--------------------------------------------------------------------------------
Gejala (laporan user): Task Proposal card "mengendap" di dasar area chat;
balasan Consultant baru justru muncul DI ATAS card. Card tidak ikut naik.

Hasil investigasi langsung (BUKAN dugaan):

(a) CSS TIDAK menjadi penyebab.
    - styles.css:2681  .consultant-proposal { flex: 0 0 auto; ... }
      => hanya ukuran natural, TIDAK keluar dari flow.
    - Tidak ada `margin-top:auto` sama sekali di styles.css (pencarian = 0 hasil).
    - Tidak ada `position:absolute|fixed`, `order`, atau `align-self` pada
      .consultant-proposal.
    - styles.css:1113  .consultant-messages { flex:1 1 auto; min-height:0;
      overflow-y:auto; display:flex; flex-direction:column; gap:12px }
      => tidak ada `justify-content` (flow normal, bukan space-between/flex-end).
    Kesimpulan: kontrak layout benar; card SECARA CSS berada di dalam flow.

(b) Penyebab SEBENARNYA = STRUKTUR DOM di ConsultantChat.vue.
    Sebelum perbaikan, template dirender sebagai:
        <div class="consultant-messages" ref="scroller">
          <div v-for="(msg, i) in messages" ...>                      <-- pesan
             ... (bubble user/assistant, tools) ...
          </div>
          <div v-if="sending" class="consultant-thinking">…</div>
          <div v-if="lastProposal" class="consultant-proposal"> … </div>   <-- DI LUAR v-for
        </div>
    => Card di-render di LUAR loop `v-for`, sebagai child TERAKHIR container.
       Akibatnya card SELALU di-append setelah SELURUH daftar pesan, terlepas
       dari pesan mana yang menghasilkan proposal. Saat user mengirim pesan
       lanjutan, balasan baru masuk ke dalam `v-for` (sehingga muncul DI ATAS),
       sementara card tetap berada di posisi terakhir container (DI BAWAH).
       Inilah yang membuat card tampak "terpaku di dasar".

CATATAN: card TIDAK pernah benar-benar out-of-flow (tidak absolute/fixed).
Masalahnya adalah POSISI RENDER statis (selalu terakhir), bukan `position` CSS.


--------------------------------------------------------------------------------
2. PERBAIKAN YANG DITERAPKAN (frontend-only, minimal, terarah)
--------------------------------------------------------------------------------
File diubah: web/frontend/src/components/ConsultantChat.vue (satu file).

(1) Memindahkan blok Task Proposal KE DALAM loop `v-for="(msg, i) in messages"`,
    tepat setelah konten pesan (bubble + tools), diterapkan hanya untuk pesan
    assistant yang memiliki proposal:

        <div v-if="msg.role === 'assistant' && msg.taskProposal" class="consultant-proposal">
          <div class="cp-head">
            <span class="cp-title">Task Proposal</span>
            <button class="run-task-btn" type="button" :disabled="running"
                    @click="runTask(msg.taskProposal)"> … </button>
          </div>
          <pre class="cp-body">{{ msg.taskProposal }}</pre>
        </div>

    => Card kini mengalir secara KRONOLOGIS: ia muncul persis setelah pesan
       yang menghasilkannya. Balasan Consultant berikutnya (pesan baru di
       `v-for`) otomatis muncul DI BAWAH card; card "naik ke atas" seperti
       bubble biasa.

(2) Menghapus blok `v-if="lastProposal"` yang dulu dirender di luar loop
    (sumber duplikasi posisi statis).

(3) `runTask(proposal)` kini menerima proposal dari pesan terkait
    (`@click="runTask(msg.taskProposal)"`), dengan fallback ke
    `lastProposal.value` bila argumen tidak ada — sehingga perilaku Run Task
    tetap sama:

        function runTask(proposal) {
          const target = proposal || lastProposal.value;
          if (!target || props.running) return;
          emit("run-task", target);
        }

CATATAN: `.consultant-proposal` TIDAK dipindahkan keluar dari
`.consultant-messages`, tidak dibuat fixed/absolute/order, dan tidak diubah
CSS-nya. Perubahan murni pada urutan render DOM.


--------------------------------------------------------------------------------
3. KONTRAK LAYOUT YANG DIPERTAHANKAN (tidak diubah)
--------------------------------------------------------------------------------
- .consultant-messages  = SATU-SATUNYA scroll owner (overflow-y:auto,
                          flex:1 1 auto, min-height:0). TIDAK diubah.
- .consultant-foot      = tetap SIBLING dengan flex-shrink:0 (composer menempel
                          di bawah; sudah benar, tidak diubah).
- .consultant-proposal  = tetap DI DALAM .consultant-messages; tidak fixed/
                          absolute; tidak dipindah keluar.
- styles.css            = TIDAK diubah sama sekali untuk task ini.


--------------------------------------------------------------------------------
4. FITUR LAIN YANG TIDAK DIUBAH (no regression)
--------------------------------------------------------------------------------
- Mode Quick/Investigate: tidak diubah.
- Provider/Model select: tidak diubah.
- Attach image (paperclip, preview, kirim): tidak diubah.
- Run Task: perilaku sama (emit "run-task"); hanya sumber argumen proposal yang
  kini berasal dari pesan terkait, dengan fallback tetap.
- Disable/Label "Running…" saat task berjalan: tetap (prop `running`).
- Tool activity badges: tidak diubah.
- Keyboard Enter=newline / Shift+Enter=send: tidak diubah.
- Auto-scroll (watch messages + nextTick): tidak diubah; scroller tetap
  `.consultant-messages`.
- Backend/API/queue Agent: TIDAK disentuh.


--------------------------------------------------------------------------------
5. VALIDASI
--------------------------------------------------------------------------------
- Build frontend:
    cd web/frontend
    node node_modules/vite/bin/vite.js build
  => "31 modules transformed", "built in …", 0 error.

- Verifikasi bundle dist (bukan hanya asumsi):
    * JS bundle memuat `consultant-proposal`.
    * Pemeriksaan node render compiled menunjukkan kunci berurutan pada SATU
      node pesan: key:0 cmsg-images / key:1 ctext / key:2 md(innerHTML) /
      key:3 consultant-tools / key:4 consultant-proposal
      => membuktikan .consultant-proposal kini anak di DALAM node pesan
         (di dalam v-for), bukan lagi item terpisah di akhir container.
    * CSS bundle memuat:
        .consultant-proposal { … flex:0 0 auto … }        (utuh)
        .consultant-messages { … overflow-y:auto … }        (scroll owner utuh)
        .consultant-foot { … flex-shrink:0 … }              (composer utuh)

- Scope check:
    git status --porcelain  => hanya:
        M web/frontend/src/components/ConsultantChat.vue
    git diff --numstat -- src web/django_app => KOSONG
    => Tidak ada perubahan backend/Python/API/queue.


--------------------------------------------------------------------------------
6. FILE YANG DIUBAH
--------------------------------------------------------------------------------
[1] web/frontend/src/components/ConsultantChat.vue
    - Memindahkan blok Task Proposal ke dalam loop pesan (inline, kronologis).
    - Menghapus blok lastProposal di luar loop.
    - runTask(proposal) menerima proposal pesan terkait + fallback.

[2] new_analisa.txt (file ini) — ringkasan hasil.

Tidak ada perubahan pada: styles.css, App.vue, api.js, ConsultantService/API,
orchestrator, task queue, atau file Python lainnya.


--------------------------------------------------------------------------------
7. CATATAN / SISA
--------------------------------------------------------------------------------
- Verifikasi akhir yang bersifat manual (browser): kirim Task Proposal, lalu
  kirim pesan lanjutan untuk memastikan card tergeser ke atas dan balasan baru
  muncul di bawah. Build produksi sudah memuat logika baru; jalankan
  run.bat / rebuild+serve dan lakukan hard-refresh browser (cache dist lama
  pernah menjadi penyebab gejala serupa sebelumnya).
- Tidak ada test otomatis untuk interaksi UI ini; validasi mengandalkan build
  0 error + pemeriksaan bundle dist.
================================================================================
